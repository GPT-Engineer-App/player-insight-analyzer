import { Box, Heading, Text } from "@chakra-ui/react";

const Integration = () => (
  <Box p={4}>
    <Heading>Integration</Heading>
    <Text mt={4}>There shall be possible API integration into the platform in the nearest future.</Text>
  </Box>
);

export default Integration;